{

    for i:=0..12;1 {

    }

    for i:=0..12 {

    }

    if b := number_guess() ? bool {
        out("we got " + b)
    }elif s := _?string {
        out(s)
    }elif err := _?error {
        out(err.message)
        out(err.traceback)
    }else {}

}