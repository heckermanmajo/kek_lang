- define all structs on top level
  only add a prefix to the struct name

- we can translate methods into methods

- add a prefix to all definitions

- we translate . access to -> access

- function calls can eb simple function calls